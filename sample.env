# ════════════════════════════════════════════════
#   ISHU MUSIC BOT — Environment Variables
#   Copy this to .env and fill in your values
# ════════════════════════════════════════════════

# ── Telegram App Credentials ──────────────────
# Get from https://my.telegram.org/apps
API_ID=
API_HASH=

# ── Bot Token ─────────────────────────────────
# Get from @BotFather on Telegram
BOT_TOKEN=

# ── Log Channel ───────────────────────────────
# Numeric chat ID of your log channel (bot must be admin)
LOGGER_ID=

# ── Telegram Dump / Storage Channel (Optional) ──
# Dedicated channel ID to store downloaded audio/video file_ids for instant cached playback.
# Defaults to LOGGER_ID if not specified.
STORAGE_GROUP_ID=

# ── MongoDB ───────────────────────────────────
# Get a free cluster at https://cloud.mongodb.com
MONGO_URL=

# ── Owner ─────────────────────────────────────
# Your Telegram numeric user ID
OWNER_ID=

# ── Pyrogram Sessions ─────────────────────────
# Get string sessions from @StringFatherBot on Telegram
# At least SESSION (SESSION1) is required
SESSION=

# ── YouTube Download Method (Railway API) ─────
# Railway self-hosted YouTube API proxy
RAILWAY_YT_API_URL=https://titanic-api-v3-01462a8481af.herokuapp.com
RAILWAY_YT_API_KEY=titanic_lhQkzaBhIQTwpquq_XBIfBI52wtN49fhdTOBBBkfLNo

# ── Optional Settings ─────────────────────────
# Maximum song duration allowed (in minutes, default: 120)
DURATION_LIMIT=120

# Maximum songs in queue per group (default: 20)
QUEUE_LIMIT=20

# Maximum songs fetched from a playlist (default: 20)
PLAYLIST_LIMIT=20

# Enable thumbnail generation (True/False, default: True)
THUMB_GEN=True

# Enable video play mode (True/False, default: True)
VIDEO_PLAY=True

# Auto leave voice chat when queue is empty (True/False, default: False)
AUTO_LEAVE=False

# Auto end stream when no one in VC (True/False, default: False)
AUTO_END=False

# ── Auto cleanup (fixes "bot works slow") ─────
# A full disk silently breaks yt-dlp/ffmpeg downloads. When free disk space
# drops below CLEANUP_DISK_THRESHOLD%, orphaned cached media (not referenced
# by any active/queued track) is deleted. Scans every CLEANUP_INTERVAL secs.
AUTO_CLEANUP=True
CLEANUP_DISK_THRESHOLD=20
CLEANUP_INTERVAL=1800

# ── Log group reporting ───────────────────────
# Forward detailed play logs to the log group on every /play (PLAY_LOG) and
# forward playback/download errors with tracebacks (ERROR_LOG).
PLAY_LOG=True
ERROR_LOG=True

# Bot default language code (default: en)
LANG_CODE=en

# Support links (shown in error messages)
SUPPORT_CHANNEL=https://t.me/titanic_network
SUPPORT_CHAT=https://t.me/+WAOT47P-70QwOTBl

# ── Image Customization ───────────────────────
DEFAULT_THUMB=https://te.legra.ph/file/3e40a408286d4eda24191.jpg
PING_IMG=https://files.catbox.moe/haagg2.png
START_IMG=https://files.catbox.moe/zvziwk.jpg